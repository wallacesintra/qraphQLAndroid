mutation Login($input: LoginInput!) {
    login(input: $input) {
        token
        user {
            id
            firstName
            lastName
            email
            role
            phone
            gender
            trained
            organizationId
        }
    }
}