mutation CreateCamp($input: CreateCampInput!) {
    createCamp(input: $input) {
        id
        name
        startDate
        endDate
        createdAt
        curriculum
        organizationId
        schoolId
    }
}